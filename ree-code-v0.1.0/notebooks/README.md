# Notebooks

No notebook is required for the v0.1.0 reference results. Executable scripts are
preferred because they are easier to test and reproduce. Future notebooks must
state their scientific status and should call package functions rather than
contain untested duplicate implementations.
